version: '3'
services:
  postgres:
    container_name: azdev_pgblank
    image: postgres:12.2-alpine
    expose:
      - 5432
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_PASSWORD=password
