/* eslint-disable @typescript-eslint/no-var-requires */
// const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    container: {
      center: true,
    },
    extend: {
      colors: {
        links: '#2979FF',
      },
    },
  },
  variants: {},
  plugins: [],
};
